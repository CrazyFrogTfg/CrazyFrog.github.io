import { NamePlaylistPipe } from './name-playlist.pipe';

describe('NamePlaylistPipe', () => {
  it('create an instance', () => {
    const pipe = new NamePlaylistPipe();
    expect(pipe).toBeTruthy();
  });
});
